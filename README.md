Batch script to fix Dead Redemption 2 Photo Mode files
How to Use:
Download fix_images.bat. 
Place the fix_images.bat file in the folder where the images are located. 
Open Command Prompt (CMD). Navigate to the folder where fix_images.bat is located using the cd command (for example, cd C:\path\to\folder). 
Type fix_images.bat and press Enter.
